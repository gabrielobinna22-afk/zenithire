# Zenithire uses two separate env files, one per app — copy each of these:
#
#   backend/.env.example   → backend/.env
#   frontend/.env.example  → frontend/.env.local
#
# This file exists only so the root of the repo isn't missing one; it is
# not read directly by either app.
